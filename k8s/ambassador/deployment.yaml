apiVersion: apps/v1
kind: Deployment
metadata:
  name: ambassador
  namespace: jumo-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ambassador
  template:
    metadata:
      labels:
        app: ambassador
    spec:
      containers:
        - name: ambassador
          image: ghcr.io/kristy/jumo-ambassador:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          resources:
            requests: { cpu: "50m",  memory: "64Mi"  }
            limits:   { cpu: "200m", memory: "256Mi" }
          envFrom:
            - configMapRef:  { name: jumo-config }
            - secretRef:     { name: jumo-secrets }
          env:
            - name: DB_HOST
              value: ambassador-db
            - name: DB_NAME
              value: ambassador
          livenessProbe:
            httpGet: { path: /api/ambassador, port: 8000 }
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet: { path: /api/ambassador, port: 8000 }
            initialDelaySeconds: 5
            periodSeconds: 10
